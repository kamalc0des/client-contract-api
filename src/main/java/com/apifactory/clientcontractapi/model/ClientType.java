package com.apifactory.clientcontractapi.model;

/**
 * Enum representing the type of client.
 * Used to simplify the filter on type when we have a list of Client for example. Good to add ✅
 */
public enum ClientType {
    PERSON,
    COMPANY
}
